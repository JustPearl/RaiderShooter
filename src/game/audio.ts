/* Restored from https://github.com/JustPearl/RaiderShooter (main).
   Re-exports the audio module verbatim so the App and the engine share
   the exact same singleton instance. */
export { SFX, sfx } from "sandbox-workspace/src/game/audio";
